export enum Config {
  TSCONFIG = 'tsconfig.json',
}

export enum Commands {
  BASE_URL = 'base-url',
  ADD_PATH = 'add-path',
  CLEANUP = 'cleanup',
  CONFIGURE = 'configure',
}

export enum PathTypes {
  FOLDER = 'folder',
  FILE = 'file',
}
