export enum Config {
  TSCONFIG = 'tsconfig.json',
  JSCONFIG = 'jsconfig.json',
}

export enum Commands {
  BASE_URL = 'base-url',
  ADD_PATH = 'add-path',
  CLEANUP = 'cleanup',
  CONFIGURE = 'configure',
}
